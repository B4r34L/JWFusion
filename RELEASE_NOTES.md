# Release notes

Play Console release notes, kept here so there's a history outside the
console itself. Newest first. Keep each entry under ~500 characters (Play's
per-language limit on the release notes field).

## 0.1.0+1 — Internal testing (first build)

Initial internal test build of JW Fusion.

- Merge multiple .jwlibrary backups into one file — every note, highlight,
  bookmark, and tag is kept
- Conflicts between backups resolve automatically (most recent edit wins)
- Runs fully offline; nothing leaves your device

This is an early build for testing. Please report anything that looks off,
especially after importing the merged file back into JW Library. Thanks for
helping test!
